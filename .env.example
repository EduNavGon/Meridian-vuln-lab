# Meridian Ledger — environment configuration
# Railway injects PORT automatically; you normally do not need to set it.
PORT=3000

# Secret used to sign the session cookie. Set a random value in production-like envs.
SESSION_SECRET=change-me-in-your-lab

# Set to "1" only in the isolated lab. Gates a warning banner, not the vulnerabilities.
LAB_MODE=1
